[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I", "UP", "B", "SIM", "PLW", "BLE"]

[tool.ruff.lint.per-file-ignores]
"fast_api/**/*.py" = ["B008"]